# Cron Schedule Flow

Start **CronScheduleFlow** with **CronScheduleInput**. Its interval uses minutes,
hours, or days and its run count limits occurrences. The next durable timer is
scheduled when work starts. Publish to **trigger** to run now or **skip** to
consume the pending occurrence.
