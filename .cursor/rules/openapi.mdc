---
description: OpenAPI-first frontend and backend contract
globs: "{api,internal/api,web/src/api}/**/*"
alwaysApply: false
---

# OpenAPI rules

`api/openapi.yaml` is the only HTTP contract source. Generate Go transport code
with ogen and the TypeScript Fetch client with Hey API. Commit generated files
and enforce zero drift. Handwritten code uses explicit domain mappers.

Snapshot is the only durable read path. The four legacy read paths must remain
absent, and tests must enforce their absence.
